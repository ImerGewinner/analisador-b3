name: Atualizar dados e publicar

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "*.py"
      - "requirements.txt"
      - ".github/workflows/update.yml"
      - "docs/index.html"
      - "docs/app.js"
      - "docs/styles.css"
  schedule:
    # 01:37 UTC de terça a sábado = 21:37 de segunda a sexta em Manaus.
    - cron: "37 1 * * 2-6"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "analisador-b3-pages"
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Instalar dependências
        run: python -m pip install -r requirements.txt

      - name: Testar regras de proventos e Bazin
        run: python -m unittest test_dividends.py

      - name: Atualizar mercado B3 e fundamentos CVM
        run: python run_pipeline.py

      - name: Reconciliar CNPJ e regenerar painel
        run: python fix_cnpj.py

      - name: Importar LPA por classe da DRE CVM
        run: python eps_loader.py

      - name: Atualizar proventos, payout, DY e Bazin
        run: python run_dividends.py

      - name: Registrar banco e JSON atualizados
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/analisador.db docs/data.json docs/status.json
          if git diff --cached --quiet; then
            echo "Sem alterações para registrar."
          else
            git commit -m "dados: atualização automática"
            git push
          fi

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Empacotar site
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: update
    steps:
      - name: Publicar GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
